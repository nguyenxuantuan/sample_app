<% provide :title, t("all_users") %>
<h1><%= t "all_users" %></h1>
<%= paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>
<%= paginate @users %>
